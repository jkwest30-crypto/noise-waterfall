{
  "name": "waterfall-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:install": "playwright install chromium",
    "engine": ".venv/bin/python -m uvicorn waterfall.api.app:app --port 8317",
    "dev:all": "bash scripts/dev.sh --no-open",
    "start": "bash scripts/dev.sh"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^26.1.1",
    "@types/three": "^0.185.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
